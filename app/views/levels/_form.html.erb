<%= form_for(@level) do |f| %>
  <% if @level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@level.errors.count, "error") %> prohibited this level from being saved:</h2>

      <ul>
      <% @level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hex %><br>
    <%= f.text_field :hex %>
  </div>
  <div class="field">
    <%= f.label :chib_count %><br>
    <%= f.number_field :chib_count %>
  </div>
  <div class="field">
    <%= f.label :found_by %><br>
    <%= f.text_field :found_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
